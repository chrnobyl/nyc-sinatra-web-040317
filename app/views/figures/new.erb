<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Create a new figure:</h1>
    <form action="/figures" method="POST">
    Figure Name: <input id="figure_name" type="text" name="figure[name]">

    Title id: <input type="text" name="figure[title_ids][]">

    Landmark id: <input type="text" name="figure[landmark_ids][]">

    Landmark name: <input id="new_landmark" type="text" name="landmark[name]">

    Title name: <input id="new_title" type="text" name="title[name]">

    <% Landmark.all.each do |landmark| %>
    <label><%= landmark.name %></label>
    <input id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" type="checkbox" value="<%= landmark.id %>"
    <% end %>

    <% Title.all.each do |title| %>
    <label><%= title.name %></label>
    <input id="title_<%= title.id %>" name="figure[title_ids][]" type="checkbox" value="<%= title.id %>"
    <% end %>

    </form>

    <input type="submit" value="Create New Figure">

  </body>
</html>
