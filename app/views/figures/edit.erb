<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%= @figure.name %></h1>
    <form action="/figures/<%=@figure.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <input id="figure_name" type="text" name="figure[name]" value="<%= @figure.name %>">

      <% Landmark.all.each do |landmark| %>
          <label><%= landmark.name %></label>
          <input id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" type="checkbox" value="<%= landmark.id%>"<%= 'checked'  if @figure.landmarks.include?(landmark)%>
      <% end %>
      <br>
      <input id="new_landmark" type="text" name="landmark[name]" placeholder="new landmark">
      <br>
      <% Title.all.each do |title| %>
          <label><%= title.name %></label>
          <input id="title_<%= title.id %>" name="figure[title_ids][]" type="checkbox" value="<%= title.id%>"<%= 'checked'  if @figure.titles.include?(title)%>
      <% end %>
      <br>
      <input type="submit" value="Edit Figure">
    </form>

  </body>
</html>
